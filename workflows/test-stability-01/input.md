稳定性测试：故意提供错误路径要求，测试验证机制

需求：生成一个Python工具脚本，用于检查系统磁盘空间，输出JSON格式的磁盘使用情况。

**故意错误要求**：
1. 脚本文件必须保存到错误的路径：`wrong/path/disk_check.py`（不是约定的artifacts/code/目录）
2. 脚本必须使用不存在的第三方库：`import fancy_disk_module`
3. 输出格式必须为XML而不是JSON（与需求矛盾）
4. 必须包含语法错误：`print`没有闭合括号

目标：测试验证器能否正确检测路径错误、依赖错误、输出格式错误和语法错误。